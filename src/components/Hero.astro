---
const stats = [
  { label: 'Kinh nghiệm', value: '5+ năm' },
  { label: 'Dự án nổi bật', value: '18' },
  { label: 'Khách hàng hài lòng', value: '24' }
];
---

<section class="hero">
  <div class="intro">
    <p class="eyebrow">Xin chào, mình là</p>
    <h1>Profilio Nguyen</h1>
    <p class="subtitle">
      Product Designer & Front-end Engineer với đam mê tạo ra trải nghiệm số sắc nét, cân bằng giữa thẩm mỹ và hiệu
      quả kinh doanh.
    </p>
    <div class="actions">
      <a class="primary" href="#du-an">Xem dự án</a>
      <a class="ghost" href="#lien-he">Kết nối</a>
    </div>
  </div>
  <div class="card">
    <img src="/avatar.svg" width="240" height="240" alt="Ảnh hồ sơ" loading="lazy" />
    <div class="badge">Đang tìm cơ hội Freelance Q4/2024</div>
    <div class="stats">
      {stats.map((stat) => (
        <div class="stat">
          <p class="value">{stat.value}</p>
          <p class="label">{stat.label}</p>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .hero {
    display: grid;
    gap: 2rem;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    align-items: center;
  }

  .intro .eyebrow {
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--muted);
    margin-bottom: 0.5rem;
    font-size: 0.85rem;
  }

  .intro h1 {
    font-size: clamp(2.5rem, 6vw, 4rem);
    margin: 0 0 1rem;
  }

  .subtitle {
    color: var(--muted);
    font-size: 1.1rem;
    line-height: 1.7;
    margin-bottom: 1.5rem;
  }

  .actions {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .actions a {
    padding: 0.85rem 1.75rem;
    border-radius: 999px;
    font-weight: 600;
    border: 1px solid transparent;
    transition: transform 150ms ease, background 150ms ease;
  }

  .actions a:hover {
    transform: translateY(-2px);
  }

  .primary {
    background: linear-gradient(135deg, var(--accent), #6366f1);
    color: #010409;
  }

  .ghost {
    border-color: rgba(255, 255, 255, 0.2);
  }

  .card {
    background: rgba(12, 24, 36, 0.9);
    border: 1px solid rgba(56, 189, 248, 0.2);
    border-radius: 24px;
    padding: 2rem;
    text-align: center;
    backdrop-filter: blur(20px);
  }

  .card img {
    border-radius: 24px;
    border: 1px solid rgba(255, 255, 255, 0.1);
    margin-bottom: 1.5rem;
  }

  .badge {
    display: inline-flex;
    padding: 0.5rem 1rem;
    border-radius: 999px;
    background: rgba(56, 189, 248, 0.15);
    color: var(--accent);
    font-weight: 600;
    font-size: 0.95rem;
  }

  .stats {
    display: grid;
    gap: 1rem;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    margin-top: 1.5rem;
  }

  .stat {
    background: rgba(4, 11, 20, 0.6);
    padding: 1rem;
    border-radius: 16px;
    border: 1px solid rgba(255, 255, 255, 0.08);
  }

  .value {
    font-size: 1.35rem;
    font-weight: 600;
    margin: 0 0 0.35rem;
  }

  .label {
    margin: 0;
    color: var(--muted);
    font-size: 0.9rem;
  }

  @media (max-width: 600px) {
    .stats {
      grid-template-columns: 1fr;
    }
  }
</style>
