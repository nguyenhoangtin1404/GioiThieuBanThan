---
const skillGroups = [
  {
    title: 'Core skills',
    level: 'Expert',
    summary: 'Product discovery, định hướng chiến lược, thiết kế hệ thống thiết kế, đo lường trải nghiệm.',
    items: ['Product discovery', 'JTBD interview', 'Design sprint', 'Design system ops']
  },
  {
    title: 'Tech stack',
    level: 'Advanced',
    summary: 'Thành thạo các công cụ thiết kế/hand-off và ngôn ngữ front-end cơ bản phục vụ prototype.',
    items: ['Figma', 'Framer', 'FigJam', 'Astro', 'React', 'TypeScript']
  },
  {
    title: 'Soft skills',
    level: 'Advanced',
    summary: 'Facilitation workshop, storytelling, mentoring designer trẻ và làm việc với stakeholder C-level.',
    items: ['Workshop facilitation', 'Stakeholder alignment', 'Mentoring', 'Bilingual presentation']
  }
];
---

<section id="ky-nang" class="section">
  <header>
    <p class="eyebrow">Kỹ năng</p>
    <h2>Stack phục vụ tăng trưởng sản phẩm</h2>
  </header>

  <div class="grid">
    {skillGroups.map((group) => (
      <article class="card">
        <div class="card-head">
          <h3>{group.title}</h3>
          <span>{group.level}</span>
        </div>
        <p class="summary">{group.summary}</p>
        <ul>
          {group.items.map((item) => (
            <li>{item}</li>
          ))}
        </ul>
      </article>
    ))}
  </div>
</section>

<style>
  .section {
    margin-top: 4rem;
  }

  header .eyebrow {
    text-transform: uppercase;
    letter-spacing: 0.2em;
    color: var(--muted);
    font-size: 0.8rem;
    margin-bottom: 0.25rem;
  }

  header h2 {
    margin-top: 0;
    font-size: clamp(2rem, 4vw, 2.8rem);
  }

  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 1.5rem;
  }

  .card {
    background: rgba(12, 24, 36, 0.88);
    border-radius: 20px;
    border: 1px solid rgba(56, 189, 248, 0.2);
    padding: 1.75rem;
  }

  .card-head {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 0.75rem;
    gap: 1rem;
  }

  .card-head span {
    padding: 0.3rem 0.75rem;
    border-radius: 999px;
    border: 1px solid rgba(255, 255, 255, 0.15);
    font-size: 0.8rem;
    color: var(--muted);
  }

  .summary {
    color: var(--muted);
    font-size: 0.95rem;
    line-height: 1.5;
    margin: 0 0 1rem;
  }

  ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  li {
    padding: 0.45rem 0.85rem;
    border-radius: 12px;
    background: rgba(255, 255, 255, 0.05);
    font-size: 0.9rem;
  }
</style>
