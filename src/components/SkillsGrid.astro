---
const skillGroups = [
  {
    title: 'Frontend',
    level: 'Advanced',
    summary: 'Xây dựng SPA/SSR với Angular & TypeScript, UI component và tối ưu hiệu năng/SEO.',
    items: ['Angular', 'TypeScript', 'RxJS', 'TailwindCSS', 'Astro']
  },
  {
    title: 'Backend',
    level: 'Advanced',
    summary: 'Microservices, ký số (PKI/HSM), tích hợp hệ thống; thiết kế API, auth/RBAC, queue/cache.',
    items: ['C#/.NET', 'Java Spring', 'Python (Django/FastAPI)', 'PostgreSQL/MSSQL', 'Redis']
  },
  {
    title: 'DevOps & QA',
    level: 'Intermediate',
    summary: 'CI/CD, container hóa, giám sát; tối ưu báo cáo & chất lượng hệ thống.',
    items: ['Docker', 'GitHub Actions/Jenkins', 'NGINX', 'k6/JMeter', 'Playwright']
  }
];
---

<section id="ky-nang" class="section alt">
  <header>
    <p class="eyebrow">Kỹ năng</p>
    <h2>Stack cho full‑stack development</h2>
  </header>

  <div class="grid">
    {skillGroups.map((group) => (
      <article class="card">
        <div class="card-head">
          <h3>{group.title}</h3>
          <span>{group.level}</span>
        </div>
        <p class="summary">{group.summary}</p>
        <ul>
          {group.items.map((item) => (
            <li>{item}</li>
          ))}
        </ul>
      </article>
    ))}
  </div>
</section>

<style>
  .section {
    margin-top: 5rem;
  }
  .section.alt {
    background: var(--surface-weak);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    padding: 2rem;
  }

  header .eyebrow {
    text-transform: uppercase;
    letter-spacing: 0.2em;
    color: var(--muted);
    font-size: 0.8rem;
    margin-bottom: 0.25rem;
  }

  header h2 {
    margin-top: 0;
    font-size: clamp(2rem, 4vw, 2.8rem);
    position: relative;
  }
  header h2::after {
    content: '';
    display: block;
    width: 56px;
    height: 4px;
    border-radius: 999px;
    background: var(--accent);
    margin-top: 0.5rem;
  }

  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 1.5rem;
  }

  .card {
    background: var(--card);
    border-radius: 20px;
    border: 1px solid var(--border);
    padding: 1.75rem;
    box-shadow: var(--shadow-sm);
  }

  .card-head {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 0.75rem;
    gap: 1rem;
  }

  .card-head span {
    padding: 0.3rem 0.75rem;
    border-radius: 999px;
    border: 1px solid rgba(255, 255, 255, 0.15);
    font-size: 0.8rem;
    color: var(--muted);
  }

  .summary {
    color: var(--muted);
    font-size: 0.95rem;
    line-height: 1.5;
    margin: 0 0 1rem;
  }

  ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  li {
    padding: 0.45rem 0.85rem;
    border-radius: 12px;
    background: var(--surface-weak);
    font-size: 0.9rem;
  }
</style>
